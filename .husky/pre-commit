#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# ---------------------------------------------------
# FIX: Direct NVM path for git hook environment
# ---------------------------------------------------
export PATH="/Users/keithkoo/.nvm/versions/node/v24.11.1/bin:$PATH"

# Fallback: Load NVM if direct path fails
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Your actual validation command
npm run report:arch && npm run sentinel
